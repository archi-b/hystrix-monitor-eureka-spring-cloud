
spring:
  application:
    name: helloclient

eureka:
  instance:
    nonSecurePort: ${server.port:8072}
  client:
    serviceUrl:
      defaultZone: http://${eureka.host:localhost}:${eureka.port:8761}/eureka/